<h1>Add a project</h1>
<%= form_for @project, html:{multipart:true} do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	
	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :logo %>
	<%= f.file_field :logo %> 

	<%= f.button %>

<% end %>
